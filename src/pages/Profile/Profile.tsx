import React from 'react';
import { ProfileLayout } from 'src/layouts';
const Profile = () => {
  return (
    <ProfileLayout>
      <div>profile</div>
    </ProfileLayout>
  );
};

export default Profile;
